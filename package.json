{
  "name": "typescript-capi-thrift-experiment",
  "version": "0.0.1",
  "dependencies": {
    "@creditkarma/thrift-client": "^0.14.1",
    "@creditkarma/thrift-server-core": "^0.14.1",
    "@creditkarma/thrift-typescript": "^3.7.2",
    "replace-in-file": "^4.1.2",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "generate": "ts-node index.ts",
    "build": "cd capi-ts && yarn && yarn build"
  }
}
